# --- Core Data & ML ---
pandas==2.3.3
numpy==2.2.6
scikit-learn==1.7.2
joblib==1.5.2
scipy==1.15.2
tqdm==4.67.1

# --- PyTorch ---
# (The pip list shows CUDA 12 related packages, so we specify torch)
torch==2.9.0

# --- Transformers (Candidate 1: DeBERTa + LoRA) ---
transformers==4.57.1
peft==0.17.1
accelerate==1.3.0
bitsandbytes==0.48.2
datasets==4.3.0
huggingface-hub==0.36.0
# (safetensors is a dependency, often good to specify)
safetensors==0.6.2 

# --- Embedding Models (Candidate 2 & 3) ---
sentence-transformers==5.1.2

# --- Tree Models (Candidate 2) ---
lightgbm==4.6.0
xgboost==3.0.5